/**
 * Initialisiert die Kontakt-Seite
 * Lädt HTML-Includes und rendert dynamischen Inhalt
 */
async function initContact() {
  await includeHTML();
  // Hier können weitere dynamische Inhalte gerendert werden
  console.log("Contact page initialized");
}
