/**
 * Initialisiert die Impressum-Seite
 * Lädt HTML-Includes und rendert dynamischen Inhalt
 */
async function initImpressum() {
  await includeHTML();
  // Hier können weitere dynamische Inhalte gerendert werden
  console.log("Impressum page initialized");
}
